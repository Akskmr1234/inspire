<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Authentication and authorisation: Keycloak/OIDC integration, JWT bearer
    validation, refresh-token handling, the database-driven permission engine
    and its authorization policy provider, multi-factor authentication
    (email OTP, SMS OTP, RFC 6238 TOTP), password policy, login history and
    device tracking.
  -->

  <PropertyGroup>
    <RootNamespace>ERP.Identity</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ERP.Application\ERP.Application.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" />
    <PackageReference Include="Microsoft.AspNetCore.Authentication.OpenIdConnect" />
    <PackageReference Include="Otp.NET" />
  </ItemGroup>

</Project>
